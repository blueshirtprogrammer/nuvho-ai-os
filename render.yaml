# Render Blueprint for Agent ZERO V 2.2
# Save as render.yaml in your repo

resources:
  - name: agent-zero-x
    type: web
    region: oregon
    plan: free
    
    # Use the official Agent Zero image
    image:
      url: agent0ai/agent-zero:latest
    
    envVars:
      - key: A0_BRAND_NAME
        value: Agent ZERO V 2.2
      - key: A0_INACTIVITY_TIMEOUT
        value: "3600"
      # Add your API keys in Render dashboard (don't commit them here)
      # - key: OPENAI_API_KEY
      #   sync: false
      # - key: ANTHROPIC_API_KEY
      #   sync: false

    ports:
      - containerPort: 80
        protocol: TCP

    # Free tier health check
    healthCheck:
      path: /
      interval: 30
      timeout: 10
      retries: 3
